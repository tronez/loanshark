spring:
  cloud:
    config:
      enabled: false
  application:
    name: "accounts"
  datasource:
    driverClassName: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
